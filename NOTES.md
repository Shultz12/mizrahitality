# Build notes

Working doc for building Mizrahitality. The product spec lives in `VISION.md` and `PRD.md` — that's the source of truth for *what* and *why*. This file is *how we're going about it*: build order, decisions made, open questions.

We build feature by feature in **plan mode**: plan → approve → implement → next. No spec/audit pipeline.

## Build order

Build top to bottom; each row depends on the rows above it.

| # | Feature | Depends on | What it adds |
|---|---------|-----------|--------------|
| 1 | **monorepo-foundation** ✅ | — | pnpm workspace, `@mizrahitality/contracts`, Prisma + SQLite scaffold, Tailwind + shadcn/ui, both Next.js apps as runnable skeletons. *(Landed — `pnpm install && pnpm dev` runs both apps; plan: `plans/01-monorepo-foundation-plan.md`.)* |
| 2 | **owner-auth** ✅ | 1 | first real Prisma models (`Owner`, `Venue` — one venue per owner — `Session`); email+password sign-up / sign-in / sign-out + `httpOnly` cookie sessions; `lib/auth.ts` server helper; `(authed)/layout.tsx` route guard. *(Landed — plan: `plans/02-owner-auth-plan.md`.)* |
| 3 | **site-builder** ✅ | 2 | the builder form — venue name (→ derived slug), free-text description, image (upload **or** pick from 3 supplied stock images); persistence of inputs, a saved-content preview, an authed nav, a publish-flow skeleton (no layout/rich-text editor, no image placement, no drag-and-drop). *(Landed — plan: `plans/03-site-builder-plan.md`.)* |
| 4 | **ai-copy-and-variants** ✅ | 3 | the two Claude steps — enhance the owner's description text, then author the approved copy into each per-audience template's Rich Text slot → 7 stored `PageVariant` copy bundles at publish (all-or-nothing, with retries; AI never touches the image). *(Landed — plan: `plans/04-ai-copy-and-variants-plan.md`.)* |
| 5 | **published-page-ssr** ✅ | 4 | the owner's SSR published page (`/preview`) — the 5-zone "Warm Minimalist" design rendering a precomputed `PageVariant` copy bundle + the chosen image; adds the `RenderedPage` response DTO to `@mizrahitality/contracts` + a `lib/rendered-page.ts` builder #6 reuses. *(Landed — plan: `plans/05-published-page-ssr-plan.md`.)* |
| 6 | **analytics-api** ✅ | 4 | open REST/JSON API — `GET` rendered page by slug + visitor type, `POST` `visit` / `book-now-hover` / `book-now-click` events; an `Event` model; event request/response DTOs in `@mizrahitality/contracts`. *(Landed — plan: `plans/06-analytics-api-plan.md`.)* |
| 7 | **analytics-dashboard** ✅ | 6 | the owner dashboard — every chart/metric/percentage over recorded events (server-computed; shadcn Chart for the bar charts + trendline). *(Landed — plan: `plans/07-analytics-dashboard-plan.md`.)* |
| 8 | **customer-site** | 6 | the Next.js SSR visitor site at `/<slug>`, the mid-left demo tab (sets a visitor-type cookie; never in the URL), the "Book Now" button + confirmation |
| 9 | **demo-seed** | all | demo Owner #1 with a published venue + realistic historical analytics; demo Owner #2 starts empty |

5 ↔ 6 can interleave (both only need the stored variants from #4). 7 ↔ 8 can run in either order (both need the API from #6). Everything else is a hard chain.

## Foundation decisions (carry forward)

These were settled while planning the scaffold; keep them unless there's a reason not to.

- **Node 22 LTS**, pinned in `.nvmrc`; **pnpm `9.15.0`** pinned via root `package.json` `packageManager` (Corepack); `preinstall` runs `npx -y only-allow pnpm`. (pnpm 9, not 10 — avoids pnpm 10's `onlyBuiltDependencies` allowlist friction with Prisma/esbuild/sharp.) `.npmrc` sets `node-linker=hoisted` — a flat `node_modules` so per-package `eslint`/`tsc`/`vitest`/`prisma` resolve and the root flat ESLint config resolves its plugins.
- **Ports:** owner `5111`, customer `5112`.
- **`@mizrahitality/contracts` is consumed as TypeScript source** — no build step — so each app's `next.config.ts` needs `transpilePackages: ['@mizrahitality/contracts']`. The package has **zero runtime dependencies**: types + plain constants only.
- Contracts surface: `VISITOR_GENDERS` / `AGE_GROUPS` / `SLOT_TYPES` (`rich-text`, `image` — no title slot) / `ANALYTICS_EVENT_TYPES`, the `VisitorType` union, `allVisitorVariants()` → exactly 7, and an `ApiError` shape. DTOs (rendered-page payload, event payloads) get added by the features that introduce them. *(No API-key header constant — the API is open; see below.)*
- **Auth = cookie sessions** *(implemented in #2 — see "Open / pending")*. Sign-up / sign-in is **email + password only** (no slug at sign-up); password hashed with **`bcryptjs`** (pure JS — no native build; cost **10**); session = a 256-bit opaque `base64url` token in an `httpOnly` / `sameSite=lax` cookie (`secure` only in production — dev is `http://localhost`) backed by a `Session` row in SQLite that stores only `HMAC-SHA256(SESSION_SECRET, token)` (`SESSION_SECRET` is required); fixed 30-day expiry, no renewal; `lib/auth.ts` server helpers (`getCurrentOwner`, `requireOwner`) + `lib/auth-actions.ts` Server Actions read/write it. Routes: `src/app/(auth)/*` public, `src/app/(authed)/*` guarded by `(authed)/layout.tsx` → `requireOwner()`. No middleware, no Auth.js/NextAuth, no OAuth, no email verification, no password reset, no rate limiting.
- **One venue (one page) per owner.** No multi-venue, no venue selector. Model: `Owner` 1—1 `Venue` (or merge them). The slug is **derived from the venue name** in the builder (lowercased, spaces removed; collisions get a numeric suffix), not entered at sign-up.
- **The owner REST API is open** — no API keys, no auth header. It's consumed only by the customer app on localhost; unknown slug → 404. (Justified by the localhost-only demo scope; flagged in `README.md`.)
- **Image input:** the builder offers **3 supplied stock images** to pick from **or** an upload; uploads are stored on local disk under the owner app (path persisted in the DB as a relative key, served back via `GET /uploads/[...path]` in the owner app so the customer gets an absolute URL). The 3 stock images are supplied assets, now at `apps/owner/public/stock/{atlantis-paradise,burj-al-arab,mardan-palace}.jpg`. AI never touches the image — text only.
- **Visitor type is server-side only:** never in the customer URL or page UI. The demo tab (for reviewers) sets a cookie; the SSR render reads it and passes the type inside the server-to-server API call; absent/unknown → `neutral`.
- **UI:** TailwindCSS **v4** (CSS-based config) + shadcn/ui for owner app chrome — `shadcn init` ran in `apps/owner` only, style `new-york`, base color `neutral` (provisional — refined when the supplied owner UI lands, REQ-11). The current shadcn CLI (`shadcn@4.x`) ships **Base UI**-based components (not Radix — PRD §7 / CLAUDE.md's earlier "Radix" wording is now dated) and adds `shadcn` as a runtime dep + `@import "shadcn/tailwind.css"` to `globals.css`. shadcn Chart (Recharts) for the dashboard — data is fetched and aggregated server-side; the chart component is a thin client component that only draws the SVG from props (no client fetching, no loading state). Supplied per-audience customer templates are plain Tailwind/React components.
- **Lint/format/test tooling:** root `eslint.config.mjs` (flat config — `@typescript-eslint` recommended + `@next/eslint-plugin-next` for `apps/*`, **`no-explicit-any` = error**); each package runs `eslint .` and ESLint walks up to the root config; `next build` skips its own lint pass (`eslint.ignoreDuringBuilds` in each `next.config.ts`). Prettier (`singleQuote`, `printWidth: 100`); `.prettierignore` excludes `*.md`. Per-package Vitest, `environment: 'node'`.
- **`@mizrahitality/contracts` surface (initial):** `VISITOR_GENDERS`, `AGE_GROUPS`, `NEUTRAL_VISITOR_TYPE`, `SLOT_TYPES` (`rich-text`, `image`), `ANALYTICS_EVENT_TYPES`; `VisitorType` = string union `'<gender>-<ageGroup>' | 'neutral'` (e.g. `male-18-30`, `female-50+`; `50+` kept verbatim, URL-encoded on the wire); `allVisitorVariants()` → 7; `isVisitorType()` / `parseVisitorType()` (parse defaults to `neutral`); `ApiError` (`{ error, message }`). #4 added `copy.ts` — the copy-bundle/slot contract: `SLOT_SCHEMA_VERSION`, `CopyBundle` / `CopyStory`, `COPY_BUNDLE_CONSTRAINTS` (lenient length/word/count rules), `validateCopyBundle()` / `parseCopyBundle()` (pure, zero-dep). #5 added `page.ts` — the `RenderedPage` DTO (the SSR page payload, and #6's documented `GET …/page` response): `RENDERED_PAGE_SCHEMA_VERSION`, `BOOK_NOW_LABEL` / `POWERED_BY_TEXT` (the fixed strings), `RenderedPageTypography`, `RenderedPage` (folds the image URL + per-variant body typography + `fixed` strings in alongside the `CopyBundle`). The owner-internal `PageVariant.content` wrapper (`{ schemaVersion, copy }`) + `parsePageVariantContent` stay in `apps/owner/src/lib/page-variant.ts`; the DTO builder `buildRenderedPage` is `apps/owner/src/lib/rendered-page.ts` (#6 reuses it with a `baseUrl`). #6 added the event DTOs to `analytics.ts`: `AnalyticsEventRequest` (`{ type, visitorType, sessionId? }` — the `POST …/events` body), `AnalyticsEventResponse` (`{ ok: true }`), `ValidAnalyticsEvent` (the validated form, `sessionId: string | null`), `parseAnalyticsEventRequest` (pure validator — strict `type`/`visitorType`, lenient `sessionId`; one clear `message` per failure → used verbatim as `ApiError.message`), `isAnalyticsEventType`; the `GET …/page` response is the existing `RenderedPage` (no new alias).
- **`lib/env.ts` per app:** `apps/owner` — `DATABASE_URL` **and** `SESSION_SECRET` required (throw at module load; `SESSION_SECRET` became required in #2), `ANTHROPIC_API_KEY` **optional** (the app boots without it; enhance & publish are gated on `isAiConfigured()` and return a clear "set ANTHROPIC_API_KEY" message when empty — #4). `apps/customer` — `OWNER_API_BASE_URL` required (nothing imports it until #8). (`Owner` vs `Owner`+`Venue` was resolved in #2 — two models; see "Open / pending".)
- **Prisma + SQLite owned solely by `apps/owner`** — only `apps/owner/src/lib/prisma.ts` imports `@prisma/client`, via a dev-cached `globalThis` singleton. The customer app never touches the DB. Schema changes go through the `update-database` skill (`apps/owner/prisma/CHANGELOG.md`).
- **`.env` is gitignored; `.env.example` is committed.** `lib/env.ts` in each app fail-fasts on its required vars at module load. Owner needs `DATABASE_URL` (required), `ANTHROPIC_API_KEY`, `SESSION_SECRET`; customer needs `OWNER_API_BASE_URL` (required).
- Per-package Vitest configs (`environment: 'node'`). Tests never touch `./dev.db` — use a temp `DATABASE_URL`.
- Customer `GET /` serves a small SSR placeholder index ("visit /<your-slug>"); `GET /[slug]` SSR-renders, echoing the slug (JSX text interpolation handles HTML-escaping). The slug stub makes no owner-API call until `customer-site`.
- `pnpm db:migrate` has a real migration history as of #2 (`apps/owner/prisma/migrations/`); `pnpm db:push` is no longer used.
- `scripts/seed.mjs` is a plain-Node-ESM no-op stub until `demo-seed`.
- No project-level `.claude/` directory; no i18n machinery.
- Root scripts: `dev` / `build` / `lint` / `typecheck` / `test` / `format` / `format:check` via `pnpm -r [--parallel]` (no `concurrently`); `db:push` / `db:migrate` / `db:studio` delegate to the owner package; `seed` runs `node scripts/seed.mjs`.

## Open / pending

- **monorepo-foundation** (#1) has landed — see `plans/01-monorepo-foundation-plan.md`. `pnpm install && pnpm dev` runs both apps; `pnpm build` / `lint` / `typecheck` / `test` / `format:check` are green.
- **owner-auth** (#2) has landed — see `plans/02-owner-auth-plan.md`. Resolved decisions:
  - **`Owner` vs `Owner`+`Venue`** → **two models**. Minimal `Venue` now (`ownerId @unique` + timestamps, `onDelete: Cascade`); content fields (name/slug/description/image) get added by #3 via `update-database`. `Session` (token-HMAC, owner FK, `expiresAt`) is the third model. First real migration: `apps/owner/prisma/migrations/20260512093834_add_owner_venue_session/` — `prisma migrate dev` from here on (not `db push`).
  - **Session lifetime** → fixed **30-day** expiry set at sign-in, **no renewal logic** (demo-only). `expiresAt` checked on every lookup (expired → row deleted, treated as signed-out); the next sign-in mints a fresh 30-day session.
  - **Session token** → 256-bit `randomBytes(32)` opaque token, `base64url`; the DB stores `HMAC-SHA256(SESSION_SECRET, token)` as `Session.tokenHash @unique`; the cookie holds the raw token. → **`SESSION_SECRET` is now `required(...)` in `apps/owner/src/lib/env.ts`**; `apps/owner/.env.example` ships a throwaway dev value (re-`cp` `.env.example` → `.env`, or the app won't boot — by design).
  - **Password hashing** → `bcryptjs` (pure JS, Windows/pnpm-friendly), cost **10** (the "Foundation decisions" bullet above is amended accordingly).
  - **Error messaging** → sign-in returns a **generic** `"Invalid email or password."` (no account-existence leak; ~equal time spent in the not-found branch); sign-up returns a **clear** `"That email is already registered…"` on the email field (REQ-1 — sign-up inherently reveals existence).
  - **Auth transport** → React **Server Actions** (`signUpAction` / `signInAction` / `signOutAction`), not `/api/auth/*` route handlers. **No middleware** — `(authed)/layout.tsx` → `requireOwner()` is the security boundary; a cookie-presence Edge fast-path was deliberately deferred.
  - **`cuid()` ids**, `@@map` snake-case table names (`owners` / `venues` / `sessions` / `page_variants`).
- **site-builder** (#3) has landed — see `plans/03-site-builder-plan.md`. Resolved decisions:
  - **Second migration** — `apps/owner/prisma/migrations/20260512102303_add_venue_content_and_page_variant/`. Adds the `Venue` content columns (`name`, `slug @unique`, `description @default("")`, `imageKind` + `imageValue`) + the publish-flow skeleton (`publishState @default("draft")` — `draft`/`publishing`/`published`, plain string, owner-internal; `slugLockedAt DateTime?`; `publishedAt DateTime?`) and the `PageVariant` model (`venueId`, `visitorType`, `content Json`, `@@unique([venueId, visitorType])`, `@@index([venueId])`). SQLite can't add non-null columns to a non-empty table so the migration redefines `venues`; in practice `venues` is empty pre-#3 → non-destructive. If `prisma migrate dev` ever balks, wipe the disposable `dev.db*` and re-run.
  - **`PageVariant` is created but empty in #3** — `content` is `Json` (TEXT/JSONB on SQLite), shape **intentionally unmodeled** until #4 (`ai-copy-and-variants`), which populates the 7 rows and the real Publish action.
  - **Slug freeze** → **frozen at first publish** via `Venue.slugLockedAt`. Before that, renaming re-derives the slug freely (lowercase the name, strip whitespace, numeric-suffix collision loop in `lib/slug.ts`; `slug @unique` is the DB backstop, `P2002` caught + retried once). After, the slug is immutable and shown read-only. `publishAction` sets `slugLockedAt: existing ?? new Date()`.
  - **Venue-name validation** → English letters + spaces only (`^[A-Za-z]+( [A-Za-z]+)*$`), 1–60 chars, internal whitespace collapsed (`lib/validation.ts`); a clear rejection message naming what's allowed. Description: free text, trimmed, ≤ 5000 chars, may be empty.
  - **Upload constraints** → `image/jpeg` / `image/png` / `image/webp`, ≤ **5 MB**, **no** resizing/cropping/optimizing (no `sharp`); validated by declared `File.type` **plus** a magic-byte sniff (`lib/uploads.ts`); server-generated random-hex filename. Next's built-in `FormData`/`File` handles the multipart upload in the Server Action — no `formidable`/`multer`.
  - **Uploads on disk** → `apps/owner/uploads/venues/<venueId>/<random>.<ext>` — **outside `public/`** and the build, **gitignored**, `mkdir -p`'d on demand (no `.gitkeep`). Persisted as the relative key `venues/<venueId>/<file>` in `Venue.imageValue` (with `imageKind: 'upload'`). Served by an **unauthenticated** owner Route Handler `GET /uploads/[...path]` (`app/uploads/[...path]/route.ts`, deliberately outside `(authed)` so the public customer page can load it; path-traversal-guarded, keys are unguessable random hex).
  - **3 supplied stock images** → moved to `apps/owner/public/stock/{atlantis-paradise,burj-al-arab,mardan-palace}.jpg` (committed app assets), registered by stable id in `lib/stock-images.ts`; a venue picking one stores `imageKind: 'stock', imageValue: <id>`.
  - **No `@mizrahitality/contracts` additions** in #3 — image kinds + publish state are owner-internal; the rendered-page DTO is #5's, the slot schema #4's.
  - **Builder transport** → Server Actions (`saveVenueAction` / `publishAction` in `lib/builder-actions.ts`) invoked from `<form action>` (matches the #2 auth pattern), not `/api/*` routes. The form `router.refresh()`es after a save so the Server-Component `<VenuePreview>` (which reflects the **saved** venue) re-renders; a client-side slug preview updates as the owner types via the pure `deriveSlugBase`. No-JS form-error fallback is a known limitation (carried from #2).
  - **Routes** → `(authed)/builder` is the builder page; `(authed)/dashboard` now shows a "Your venue" card (or a "Create your venue" CTA) + an "Analytics arrives in #7" card; `(authed)/layout.tsx` gained a small authed nav (wordmark + Dashboard/Builder links + Sign out — the standalone sign-out form moved here).
  - **Follow-up** — #6's REST API will need an owner public base URL to absolutize the image URL (`/uploads/...`, `/stock/...`) in the payload; #5 resolved its half by keeping `RenderedPage.imageUrl` app-relative for in-app rendering and giving `buildRenderedPage` an optional `baseUrl` arg that #6 will pass.
- **ai-copy-and-variants** (#4) has landed — see `plans/04-ai-copy-and-variants-plan.md`. Resolved decisions:
  - **Scope = data layer only** — the copy-bundle/slot schema in `@mizrahitality/contracts` (`copy.ts`: `SLOT_SCHEMA_VERSION`, `CopyBundle` / `CopyStory`, `COPY_BUNDLE_CONSTRAINTS`, `validateCopyBundle` / `parseCopyBundle` — pure, zero-dep); a templates/copy-rules registry the AI step consumes (`apps/owner/src/lib/templates.ts` — **verbatim transcription** of `plans/copy-rules/_BASE-PROMPT.md` + the 7 `*_Rules.md` persona blocks + the body-typography rows from `_TEMPLATE-STYLING.md`; the `plans/copy-rules/` files stay the source of truth, a test asserts coverage); the two Claude steps (`apps/owner/src/lib/ai/` — a lazily-constructed `Anthropic` client + `enhanceDescription` + `generateVariantCopy` + `generateAllVariants`); deterministic code-side parsing of the AI's JSON into the bundle (no second LLM call to route text); structural + length validation; the publish gate; per-variant regenerate; storing the 7 `PageVariant.content` blobs. The per-audience React render (the 5-zone page from `plans/site-design/code.html` + `DESIGN.md`, translated to Tailwind v4) is **#5**.
  - **Composition** — `_BASE-PROMPT.md` describes "[ base ] + [ one persona file ] + the venue NAME/DESCRIPTION as the user turn"; our Messages-API mapping keeps the same three pieces in order but puts base + persona into `system` blocks (so the base is a cacheable prefix, the persona the varying tail) and the venue data in the `user` turn (`VENUE_NAME: …\nVENUE_DESCRIPTION:\n…`). Recorded in `lib/templates.ts`'s header.
  - **`PageVariant.content` shape** = the owner-internal wrapper `{ schemaVersion: 1, copy: CopyBundle }` (`apps/owner/src/lib/page-variant.ts` — `PageVariantContent` + `parsePageVariantContent`); the `CopyBundle` type + validator are in contracts (shared with #5/#6). **No Prisma migration** — `PageVariant` already exists from #3; the schema version lives inside the JSON blob, not as a column.
  - **Publish failure handling** = stateful `useActionState` action (`publishAction` → `runPublishPipeline` in `apps/owner/src/lib/publish.ts` — kept out of the `'use server'` module so it can take an injected client for the integration test); `publishState:'publishing'` while in flight; ~2 retries per variant on (JSON parse failure | `variant` mismatch | structural/length validation failure), the retry turn appending the prior errors; commits transactionally (`deleteMany` then re-create the 7 then `venue.update`) only if all 7 validate; **all-or-nothing** — any failure (incl. a network error) changes nothing, reverts `publishState`, reports `variantErrors`; a failed re-publish leaves the live page intact.
  - **Empty description at publish** = **blocked** ("Write a venue description before publishing."); an empty description can still be *saved* (per #3).
  - **Regenerate** = a per-variant `regenerateVariantAction` (re-validates before swapping; published venue only) **and** full Re-publish.
  - **`ANTHROPIC_API_KEY` stays optional** (`lib/env.ts`); enhance & publish are gated on `isAiConfigured()` with a clear "set ANTHROPIC_API_KEY" message; the AI client is mocked in tests; the app boots / builds / runs / tests without a key.
  - **Validation bounds are lenient by design** — the blueprint's *hard* rules are exact (tagline 10–20 words; `detailBullets` `[]` or 3–4; story = 3 non-empty paragraphs, hook 2–3 sentences, nudge 1–2); everything else gets generous char caps so a good-faith Claude reply passes first try.
  - **Model / params / caching** = `claude-sonnet-4-6`; `max_tokens` 1024 (enhance) / 1500 (variant copy); non-streaming; only the `BASE_COPY_PROMPT` system block is `cache_control: { type: 'ephemeral' }`; the 7 publish calls run **sequentially** in `allVisitorVariants()` order so the cache prefix stays warm across the batch.
  - **New dep** — `@anthropic-ai/sdk` in `apps/owner` (server-only; ships its own types). The `claude-api` skill referenced in `CLAUDE.md` **does not exist on disk** — #4 uses `@anthropic-ai/sdk` directly (Messages API, `cache_control: { type: 'ephemeral' }` for prompt caching).
  - **`vitest.config.ts`** gained `fileParallelism: false` (the integration tests share one throwaway SQLite file + clean up with unscoped `deleteMany()` in `afterEach` — files mustn't run concurrently or one file's teardown nukes another's rows mid-test) and `ANTHROPIC_API_KEY: ''` in `test.env` (so `isAiConfigured()` is deterministically false in tests regardless of the shell).
- **published-page-ssr** (#5) has landed — see `plans/05-published-page-ssr-plan.md`. Resolved decisions:
  - **Route & access** → a new top-level `apps/owner/src/app/preview/page.tsx` (`/preview`), owner-scoped: it calls `requireOwner()` itself (signed out → redirect to /sign-in) and renders the signed-in owner's one published venue — **no slug in the URL**. Deliberately **not** under the `(authed)` route group (that layout's nav chrome would contradict the design's "zero navigation"); `app/preview/layout.tsx` is a minimal shell that loads Playfair Display (600/700) + Inter (400/600) via `next/font/google` as CSS vars (`--font-playfair`, `--font-inter`) and paints the warm `#FAF7F2` background over the root body. Builder hook-ups: `PublishSection` shows a "View your published page →" link (+ a `?type=` hint) once published; `GeneratedPages` adds a per-row `Preview →` link to `/preview?type=<variant>` (variant URL-encoded — `50+` → `50%2B`).
  - **Renderer location** → built in `apps/owner` (`src/components/published-page/` — `published-page.tsx` Server Component + `book-now-button.tsx` (inert — #8 wires the click + the confirmation modal, #6 records it) + `sticky-mobile-bar.tsx` `'use client'` scroll-reveal). #6's API GET reuses the same `lib/rendered-page.ts` builder in-process (passing a `baseUrl`); #8's customer site re-implements its own renderer against the shared `RenderedPage` DTO. **No new shared package.** The two CSS-only animations (`pulse-glow`, `fade-in-up`) are plain `@keyframes` + `animate-*` utilities appended to `apps/owner/src/app/globals.css`.
  - **Variant shown** → defaults to `neutral`; `?type=<visitor-type>` on `/preview` previews the other 6 (`parseVisitorType` → `neutral` on anything unrecognised). The polished hover-out demo tab is #8's job.
  - **`RenderedPage` DTO** → `packages/contracts/src/page.ts`: `RENDERED_PAGE_SCHEMA_VERSION` (1, versioned independently of `SLOT_SCHEMA_VERSION`), `BOOK_NOW_LABEL` / `POWERED_BY_TEXT`, `RenderedPageTypography`, `RenderedPage` (`schemaVersion`, `slug`, `visitorType` = the variant *actually* served after the neutral fallback, `venueName`, `imageUrl`, `imageAlt`, `copy: CopyBundle`, `typography`, `fixed: { ctaLabel, poweredBy }`). `imageUrl` is **app-relative** (`/uploads/…` | `/stock/…`) for in-app rendering; `buildRenderedPage` takes an optional `baseUrl` that absolutizes it (`new URL(rel, baseUrl)`) for #6's API. Re-exported from `index.ts`.
  - **`lib/rendered-page.ts`** = the pure builder (`buildRenderedPage({ venue, variants, requestedType, baseUrl? })`): picks the `requestedType` row, else the `neutral` row; runs `parsePageVariantContent`; on parse failure or no row → `{ ok:false, error }` (won't happen for a normally-published venue — publish is all-or-nothing); image URL mirrors `venue-preview.tsx`'s `imageSrc`; typography from `lib/templates.ts`'s `templateEntry(servedVariant)`; `servedVariant` = the parsed bundle's `copy.variant`. Pure (no DB) → unit-tested directly (`apps/owner/src/__tests__/rendered-page.test.ts`); no HTTP-level route test (the page itself is covered by manual verification). Friendly centred notices for not-published / not-renderable instead of a bare 404; plain `<img>` for the hero (the `/uploads/*` route is dynamic — no `next/image`).
  - **No Prisma migration**, no `apps/owner/prisma/CHANGELOG.md` entry (no schema change). No AI in this path; no analytics; no REST endpoint (that's #6); no customer-site rendering (#8).
- **analytics-api** (#6) has landed — see `plans/06-analytics-api-plan.md`. Resolved decisions:
  - **The two open REST routes** live under `apps/owner/src/app/api/venues/[slug]/` — deliberately **outside `(authed)`** (open API, no keys/tokens — localhost-only demo; same posture as `app/uploads/[...path]/route.ts`). `GET …/page/route.ts` returns the precomputed `RenderedPage` payload (reuses `lib/rendered-page.ts`'s `buildRenderedPage` in-process with `baseUrl = req origin` so `imageUrl` is absolute — a proxied deploy would want an env override, out of scope; no AI — variants were generated at publish). `POST …/events/route.ts` writes one `Event` row. A `RenderedPage` JSON body, `ApiError` `{ error, message }` envelope on failures.
  - **Publish gating: GET strict, POST loose** — `GET` → `404 not_found` unless `venue.publishState === 'published'` **and** `buildRenderedPage` returns `ok` (mirrors `app/preview/page.tsx`; never leaks unpublished content). `POST` records the event against **any existing venue** regardless of publish state; only `404` if the slug doesn't resolve.
  - **`sessionId` — optional + lenient** — the customer site (#8) mints an opaque per-browser-session id and sends it on every event (powers #7's "clickers who hovered ≥ once ÷ all clickers" funnel %), but the DTO field is optional and the POST route stores `null` when absent and never 400s on a missing one (a clean superset of the README's sketched `{ type, visitorType }` body). DB column is `String?`.
  - **"Clicker who hovered ≥ once" = a `sessionId` with ≥1 `book-now-hover` event and ≥1 `book-now-click` event, order ignored** — pinned now so #7 inherits it; events with no `sessionId` are excluded from that percentage only.
  - **Validation** — `parseAnalyticsEventRequest` (in `@mizrahitality/contracts` `analytics.ts`, next to the new DTOs — pure, zero-dep, hand-rolled `{ ok, value } | { ok, message }`, no zod): `type` strict (`isAnalyticsEventType` → 400), `visitorType` strict (`isVisitorType` → 400 — a bad value in *recorded analytics* is a real bug), `sessionId` lenient. Route order: validate the body first (400 — no DB hit on garbage), then resolve the slug (404). The GET `?type` param stays lenient (`parseVisitorType` → `neutral` on `null`/garbage; `searchParams.get` already URL-decodes — `?type=female-50%2B` → `female-50+`).
  - **Minor in-plan defaults** — POST success → **`201`** with `{ ok: true }` (a row was created; not `204`); GET response sets **`Cache-Control: no-store`** (the customer SSR re-fetches per request to pick up the cookie-selected visitor type); **`baseUrl = new URL(req.url).origin`** (no new env var); **`Event` index = just `@@index([venueId])`** (demo-scale; #7 aggregates in memory); **no `Event.updatedAt`** (events are immutable); wrong HTTP method auto-405s (no handler exported).
  - **Third migration** — `apps/owner/prisma/migrations/20260512194312_add_event_model/` — a plain `CREATE TABLE "events" (...)` + `CREATE INDEX` (non-destructive, no SQLite table-redefine). Added `model Event` (`id` cuid, `venueId` + `venue` relation `onDelete: Cascade`, `type`, `visitorType`, `sessionId String?`, `createdAt`; `@@index([venueId])`, `@@map("events")`) and `events Event[]` to `model Venue`. The read-only consumer is #7. See `apps/owner/prisma/CHANGELOG.md`.
  - **No customer-app changes** — `apps/customer/src/app/[slug]/page.tsx` stays the foundation stub; #8 wires it to consume these endpoints. `apps/customer/src/lib/env.ts`'s `OWNER_API_BASE_URL` is still only consumed by #8 (nothing imports it yet).
  - **No new shared package, no zod/validation lib, no new env var.** `README.md`'s "REST API contract" section is now pinned to the implementation (REQ-21).
- **analytics-dashboard** (#7) has landed — see `plans/07-analytics-dashboard-plan.md`. Resolved decisions:
  - **Daily-visitors bar chart window = fixed last 30 calendar days** ending today, zero-filled for gap days, bucketed in the **server process's local timezone** (localhost demo — "the day the owner sees"). Events older than 30 days don't appear in the daily chart but **still count in the all-time totals/breakdowns/percentages** ("total visit count" in REQ-8 = all-time). Everything else on the page is all-time over all of the venue's events.
  - **`neutral` is omitted from the gender / age-group / clicks-by-gender charts and the conversion table** — those show only male/female × 18-30/31-50/50+ (neutral isn't an "audience"). `neutral` events still count in `totalVisits`, `bookNowClicks`/`bookNowHovers`, the daily series, the click-through %, and the hover→click funnel. Accepted consequence: when there are `neutral` events the breakdown bars sum to less than the totals — so the "total visits" card is not labelled "= sum of the bars".
  - **"Total visitors" == "total visits" == the count of `visit` events** — the denominator of percentage (a) (click-through) and of each per-segment row. Honest given `sessionId` is optional (a distinct-session count would systematically undercount).
  - **Trendline = ordinary-least-squares linear regression** over the 30-day daily-visit series (`y = slope·x + intercept`, `x` = 0-based day index), exposed as a per-day predicted value on each daily point and drawn as a recharts `<Line>` overlay inside a `ComposedChart` (Bar + Line, shared X scale). `< 2` non-trivial days → no line (n=30 here, so always a line; the all-zero series → `slope: 0`, `direction: 'flat'`).
  - **`neutralInBreakdowns` (`'omit'` | `'bucket'`) and `timeZone` (`'utc'` | `'local'`) are parameters of the pure aggregator** with defaults `'omit'` / `'local'` — the chosen behaviour is the default; a future change is one argument, not a rewrite. The aggregator (`computeDashboard`) and its `DashboardData` shape are **owner-internal** (`apps/owner/src/lib/analytics.ts`) — no `@mizrahitality/contracts` change.
  - **Charts use shadcn's Recharts-based Chart component** — `pnpm dlx shadcn@latest add chart` wrote `apps/owner/src/components/ui/chart.tsx` (reformatted to the repo's Prettier style) and added **`recharts` (3.x)** to `apps/owner/package.json`. The dashboard charts (`apps/owner/src/components/dashboard/*`) are thin `'use client'` SVG drawers fed server-computed props — no fetching, no `useEffect`/`useState`, no loading state; each renders a centered "No data yet" when its series is empty/all-zero. The greyscale `--chart-*` ramp is unchanged (trendline uses `--chart-5`, the darkest).
  - **The `(authed)` layout's `<main>` widened `max-w-3xl` → `max-w-5xl`** (~1024px) so the dashboard can use a 2-column charts grid — affects only `(authed)/dashboard` + `(authed)/builder` (sign-in/up are in `(auth)`, `/preview` is top-level — untouched).
  - **Empty-state copy** — no venue → keep the "Create your venue" CTA, a single muted "Analytics appear once you create your venue" line, no analytics block; venue + 0 events → the full layout with a muted "No analytics yet — events arrive once visitors hit your published page" note and every stat `0`, every ratio `—`, every chart "No data yet". Never an error (`computeDashboard([])` is a fully-formed zeroed object).
  - **Ratio rendering** — whole-percent (`Math.round(value·100)`%); raw counts shown as a hint so nothing is lost; zero denominator (`value === null`) → `—`.
  - **No `@mizrahitality/contracts` change, no Prisma migration / `CHANGELOG.md` entry** (the `Event` model from #6 already has everything; `@@index([venueId])` stays the only index — demo-scale, in-memory aggregation), no customer-app change. **No integration test** — the aggregator is pure and exhaustively unit-tested (`apps/owner/src/__tests__/analytics.test.ts`); the Prisma wrapper (`lib/dashboard-data.ts`) is ~8 lines of `findMany` + delegate; the page (3 states / layout / charts) is covered by manual verification (consistent with #5/#3). Added `plans/site-design/code.html` to `.prettierignore` (a supplied design mockup — shouldn't be subject to our formatter).
- **Supplied assets** — the per-audience copy-rules + prompt + template-styling assets have landed under `plans/copy-rules/` (consumed by #4) and the site-design / blueprint reference under `plans/site-design/` + `plans/landing-page/` (consumed by #5's renderer); the **3 stock images** landed in #3. The owner-facing UI (sign-up, builder, dashboard chrome) ships plain Tailwind + shadcn for now, to be refined when supplied owner-UI designs land (REQ-11) — the published page itself **is** one of the supplied designs ("Warm Minimalist").
- **App processes:** two separate Next.js processes, talking only over the (open) owner REST API — no shared DB handle. Confirmed by the architecture; nothing more to decide.

## API contract (implemented in #6 — pinned in `README.md`)

The owner app exposes it; `mizrahitality-customer` (#8) is the only consumer. **No auth** — open, no keys/tokens (localhost-only demo). Errors carry an `ApiError` body `{ "error": "<code>", "message": "<human text>" }`.

- `GET /api/venues/<slug>/page?type=<visitor-type>` → `200` the `RenderedPage` payload (the `CopyBundle` + the absolute hero `imageUrl` + per-variant body typography + the fixed UI strings) for that variant; `Cache-Control: no-store`. Unknown/absent/garbage `type` → the `neutral` variant. (`type` is supplied by the customer app server-side — it never appears in a browser URL; `50+` groups arrive URL-encoded, `?type=male-50%2B`.) Unknown slug — or a venue not in `publishState: 'published'` — → `404 not_found`. No AI in this path (variants were generated at publish, #4). Wrong method → `405`.
- `POST /api/venues/<slug>/events` with `{ "type": "visit" | "book-now-hover" | "book-now-click", "visitorType": "<visitor-type>", "sessionId"?: "<opaque>" }` → `201` `{ "ok": true }`; records the event against that venue. `sessionId` is optional (omit it → stored as `null`; never 400s on a missing one). Bad/missing `type` or `visitorType`, or a non-object/non-JSON body → `400 bad_request`. Unknown slug → `404 not_found`. **Not** gated on publish state — records against any existing venue. Wrong method → `405`.

Visitor type = `male`/`female` × `18-30`/`31-50`/`50+` (6) + `neutral` = **7**; the enum lives in `@mizrahitality/contracts`.
